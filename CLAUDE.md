# CLAUDE.md

메루카리(mercari.jp) 키워드 알림 봇입니다. GitHub Actions에서 **1분마다 스스로 돌고**,
상태 파일(`seen_items.json`)을 매 실행 `main`에 커밋합니다.

**먼저 `README.md`를 읽어 주세요.** 이 저장소의 모든 결정과 사고 경위가 거기 있습니다.
이 파일은 그 위에서 지켜야 할 작업 방식과, 이미 사고가 났던 자리만 적습니다.

문서·주석·커밋 메시지는 **한국어**로 씁니다.

## 작업 방식 (이 저장소에서 가장 중요한 두 줄)

1. **추측으로 결론 내지 마세요.** 운영 데이터나 실제 실행으로 확인하세요.
   상태 파일 이력이 1만 커밋 넘게 남아 있고, 지금까지 발견의 대부분이 거기서 나왔습니다.
2. **"문제 없음"이라고 말하기 전에, 그 확인 방법이 정말 문제를 잡아내는지 먼저
   검증하세요.** 못 잡는 도구로 이상 없다고 하면 안 됩니다.

두 번째 줄이 다섯 라운드 연속으로 가장 값진 발견을 만들었습니다.

- 시계 스캐너가 "실패 0건"을 냈을 때 과거 사고를 되살려 태워 봤더니 **두 번째 사고를
  못 잡는다**는 것이 드러났습니다.
- 감시 워크플로 설계가 실측으로 **두 번 뒤집혔습니다**(러너 없는 잡은 `runner_id`가
  0으로 오고, `total_count`가 실제 배열과 어긋나는 일이 112회 중 5회).
- 판정 감사에서는 **대조 도구 자신이 네 번 거짓 보고**를 했습니다. 그중 두 건을
  놓쳤다면 결론의 숫자가 통째로 틀렸을 것입니다.
- 시야 범위 스캔에서는 **창 크기를 재는 방법 자체가 틀렸습니다.** '순위 N번째 매물의
  나이'로 쟀더니 `30건=2.1시간치 / 60건=16.7일치 / 120건=15.5시간치`처럼 창을 넓혔는데
  깊이가 얕아지는 표가 나왔습니다. 그대로 보고했다면 "창을 줄여도 된다/안 된다"를
  아무 근거 없이 말할 뻔했습니다.
- 추천순 창을 잴 때는 **고쳐 놓은 그 방법마저 이 정렬에는 안 통했습니다.** 표가 `-3분치`
  같은 음수와 `30건=60건=120건`처럼 평평한 값을 냈습니다. 음수는 기준 시각을 잘못 잡은
  것이었고, 평평한 값은 그 계산이 서 있는 전제(컨베이어)가 깨졌다는 뜻이었습니다.
  "추천순 창은 3분치"라는 그럴듯한 오답이 표에 실릴 뻔했습니다.
- **자체 점검이 그대로 통과했습니다.** `commit_notes.py --calibrate`를 만들어 놓고 일부러
  정규식을 `\d+` → `\d`로 줄였는데 눈금이 **통과**했습니다 — 눈금에 적어 둔 숫자가 전부
  한 자리였기 때문입니다. 운영에서는 두 자리가 나오는 값이라 '12건'이 '1건'으로 세질
  뻔했습니다. **자체 점검이 있다고 안심하지 말고, 그 점검이 고장을 잡는지 고장을 내서
  확인하세요.**
- **'흔적이 없다'가 절반은 틀렸습니다.** '못 물어봄'을 상태 파일로는 못 센다고 이 파일과
  README에 적혀 있었는데, 답이 안 온 조회는 `pending_relists`에 남습니다. 못 센다고 적기
  전에 그 값을 건드리는 함수를 읽으세요 — 읽었더니 셀 수 있었고, 값은 0건이었습니다.
- **자체 점검을 고쳤다고 그 고침이 모든 칸에 들지는 않습니다.** 2026-09-15에 `\d+`->`\d`를
  겪고 "눈금에 두 자리를 섞자"로 고쳤는데 **재출품 칸에만 섞었습니다.** 2026-09-17에 고장
  15개를 다시 넣으니 눈금이 **8개를 통과**시켰습니다 — `조회실패`·`다음페이지 N회`는
  여전히 한 자리였고, 누적(`+=`->`=`)·첫 꼬리표 시각·`--first-parent`·비율의 분모도
  전부 통과했습니다. **눈금이 안 부르는 코드는 눈금이 못 지킵니다**(`walk()`·`report()`).
- **짧은 창에서 나온 비율은 '실측'이어도 결론에 쓰지 마세요.** 꼬리표가 붙는 실행을
  추정 12.6% -> "실측 38%"로 덮었는데, 그 38%가 **1.8시간짜리**였습니다. 43.3시간으로
  재니 **16.0%**로, 덮어쓴 값이 원래 추정보다 **더 틀렸습니다.** 실측이라는 이름이
  창의 좁음을 가립니다.
- **눈금 판이 넷이면 p90과 최대가 같은 값이라, 그 둘을 뭉치는 고장이 통과합니다.**
  시간 칸을 넣고 고장 23개를 넣으니 3개가 통과했는데, 둘이 이것이었고 하나는 **새 칸의
  정수 자리에 또 한 자리만 적어 둔 것**이었습니다. 자릿수는 칸마다가 아니라 **숫자마다**
  섞고, **눈금 판 수 자체가 눈금의 일부**입니다(중앙값·p90·최대·평균이 넷 다 다르게).
- **가정을 안 쓰는 자를 따로 만들고, 그 자를 아는 답에 먼저 대 봤습니다.** 같은 창을
  반복해 찍는 종단 측정입니다. 등록순에서 두 자가 ±5% 안에 맞는 것을 확인한 **다음에**
  추천순에 들이댔더니 2.7~264배로 갈렸습니다(Chrome Hearts 추정 1분 / 실측 4.4시간).
  눈금을 먼저 안 맞췄으면 "추천순도 비슷하더라"와 구별되지 않았을 것입니다.
  그리고 그 종단 측정이 **내가 도구에 적어 둔 문장 하나를 반증했습니다** — '창을 나갔다
  다시 들어온 것이 0이 아니면 체류가 정의되지 않는다'고 적었는데 등록순도 4~8건이었습니다.

- **"고쳤는데 0건"을 읽기 전에, 그 구간에 고침이 들 자리가 있었는지부터 세세요.**
  PR #37 확인에서 머지 뒤 50시간의 '어긋남 0건'을 그대로 읽을 뻔했는데, 같은 자로
  **고치기 전** 구간을 재니 거기도 0건이었습니다 — 고장 난 코드도 자기 구간에서 0을
  냅니다. 말할 수 있는 것은 **고침이 실제로 든 자리 1건**뿐이었습니다(그 한 건은
  맞았습니다). 분모를 따로 세지 않으면 '깨끗해서 0'과 '들 자리가 없어서 0'이 같아 보입니다.
- **"한 번도 안 울렸다"를 읽기 전에 울릴 자리가 몇 번이었는지 세세요.** 워치독이 배포 뒤
  4.6일 동안 취소한 실행은 0건인데, 그것은 판정이 옳아서가 아니라 **끊을 자리가 0회**였기
  때문입니다(취소된 봇 실행 1,848건 중 15분을 넘긴 것 0건). 같은 구간에 **끊으려는 시도는
  499번** 있었고 전부 잘못된 대상이었습니다 — 자리를 세지 않으면 그 499번이 '0건' 뒤에
  가려집니다.
- **13분짜리 관측으로 못 박은 성질이 4일짜리 사례에 깨졌습니다.** "`pending`이
  `?status=queued`에 들어온 적 0회(112회 관측)"가 README와 코드 주석에 근거로 박혀
  있었는데, 잡 레코드가 없는 실행 하나가 그 목록에 **4일 넘게** 들어 있었습니다.
  짧은 창은 비율만이 아니라 **'있다/없다'도 못 정합니다.**
- **도구가 실패하고도 '정상'이라고 찍는 자리를 찾으세요.** `unstick()`은 목록 조회 실패를
  None으로 갈라 놓고도, **취소 실패**는 `cancelled == 0`으로 떨어져
  `[정상] 러너를 기다리다 멈춘 실행이 없습니다`가 499번 찍혔습니다. 같은 파일 안에서도
  한쪽 경로에만 가드가 들어 있었습니다.
- **외부 API 한 번의 답으로 사람에게 알림을 내보내지 마세요.** `?status=success&per_page=1`이
  한 번 "마지막 성공이 4378분 전"이라고 답해 헛이슈가 열렸습니다 — 그때 봇은 **24초 전에**
  성공했고 26분 뒤 같은 질의는 "0분 전"이었습니다. 둘째 증인(상태 커밋 시각)을 두세요.
- **'짝이 안 맞는 알림'을 쫓았더니 자(尺)가 틀린 것이었습니다.** ⚠️ 없이 ✅만 나간
  `cadence-ok` 4건의 정체를 찾으려고 상태 파일을 한 판씩 읽었더니, 커밋 간격이 113초인데
  cadence 시계가 10분 넘게 얼어 있었습니다. 봇은 **한 판 옛날 체크아웃**과 비교하고
  있었습니다. 알림이 이상하면 판정보다 **그 판정이 쓰는 값이 어디서 왔는지**를 먼저 보세요.
- **'자리'를 세러 갔다가 더 큰 것이 나왔습니다(2026-09-18).** cadence의 자가 틀렸다는 것은
  이미 알고 있었고, 고칠지 정하려고 '낡은 체크아웃이 실제로 오탐을 내는가'의 분모를
  세러 갔습니다. 중복 신규 알림은 **0건**이었는데(자리 4,560건), 같은 자리에서 **인하
  알림은 17건이 어긋나 있었습니다.** 갈린 이유는 방어선이 **id로** 막기 때문입니다 —
  `new:{매물}`은 기준가가 안 들어가 낡은 상태로도 같은 id가 나오고, `drop:{매물}:{기준가}:{관측가}`
  는 기준가가 낡으면 **다른 id**가 되어 그대로 빠져나갑니다.
- **찾으려던 것이 아닌 곳에서 제일 큰 것이 나왔습니다.** '안 도는 경로'를 보려고 실패 실행
  목록을 열었더니, 09-14 이후 실패 17건 중 **15건이 유닛 테스트**였고 그때마다 그 1분의
  조회·전송이 통째로 결행되고 있었습니다(하루 약 4회). 원인은 판정이 아니라 시험 고정물의
  **임시 디렉터리 정리 경쟁**이었습니다.
- **상태 파일이 못 가르는 자리를 꼬리표가 가릅니다.** 낡은 사본이 마침 새 매물의 관측가와
  같으면 '사본을 물려받음'과 '아예 안 물려받음'이 **같은 기록**을 냅니다. 상태 파일만
  보면 분모가 4였는데, 꼬리표의 `없음0`(그 판에 없어짐 판정이 하나도 없었음)을 얹으니
  **92건 전부** 갈렸습니다. 두 증거를 붙일 생각을 안 하면 "아직 못 잼"으로 끝납니다.

`0건`이 나오면 **왜 0건인지** 물어보세요. 판정이 맞아서가 아니라 다른 방어선이
막고 있어서인 경우가 있었습니다(중복 신규 알림 0건 ← `sent_alerts`가 막고 있었고,
그 방어선이 못 막는 경로에서는 실제로 새고 있었습니다).

## 이미 사고가 났던 자리

| 하지 말 것 | 왜 |
|---|---|
| 시계에 의존하는 테스트를 넣기 | 봇 워크플로가 **매 실행 전에 유닛 테스트를 돌리고, 실패하면 그 실행의 조회·전송을 건너뜁니다.** 하루 중 특정 시각에만 깨지는 테스트는 곧 반복 장애입니다(2026-09-12에 두 번). 시각을 다루는 코드를 건드렸으면 `python scripts/clock_scan.py --day`와 `--repeat 100`을 **둘 다** 돌리세요 — 서로를 대체하지 못합니다. |
| `main`에 직접 고치기 | 이 봇은 1분마다 스스로 배포됩니다. `main`에 올린 그 순간이 배포입니다. **반드시 PR로** 올려 `tests.yml`을 거치세요. |
| `tests.yml`에 push 트리거 달기 | 봇이 상태 파일을 시간당 약 140번 커밋합니다. push 트리거를 달면 테스트도 140번 돌면서 봇 실행과 러너를 다툽니다. |
| 브랜치 룰셋에 `Block force pushes` / `Require status checks` 켜기 | 이력 압축이 force-push로 돌고, 봇이 체크 없는 커밋을 1분마다 `main`에 직접 push합니다. 켜면 각각 영영 실패합니다. |
| `seen_items.json` 내용을 지우기 | 이력이 줄지 않을뿐더러 `sent_alerts`를 잃으면 이미 보낸 알림이 다시 나갑니다. |
| `MAX_ITEMS_PER_KEYWORD`를 낮춰 조회를 아끼려 하기 | 한 페이지 크기 120은 **mercapi가 요청에 박아 보내는 값**이라 봇이 고를 수 없습니다. 이 상한은 '몇 건을 받을지'가 아니라 '받아 온 페이지에서 몇 건을 쓸지'입니다. 낮춰도 호출·트래픽은 그대로고, `wants_another_page()`의 '한 페이지를 가득 채웠는가'가 더 자주 참이 되어 조회가 **늘어납니다**. |
| 추천순(score) 결과에 등록순의 창 계산을 그대로 쓰기 | 그 계산은 **컨베이어 가정**(새 매물은 맨 앞으로 들어와 뒤로만 밀린다) 위에 서 있습니다. 등록순은 섭니다(최근 1시간 매물이 전부 0~54등). 추천순은 갓 올라온 매물이 **593등**에 꽂히고, 깊이가 창 크기와 무관하게 평평해집니다(30건=60건=120건). `coverage_scan`이 두 신호에 ⛔를 답니다 — 뜨면 표를 읽지 말고 `--snapshots`로 종단 측정하세요. |
| 여러 정렬 패스의 나이를 **스캔 시작 시각 하나로** 재기 | 뒤에 도는 패스일수록 기준이 과거라 나이가 음수로 나옵니다. 나이는 **그 키워드를 조회하고 난 시각**으로 재세요. `format_span`이 음수를 '⛔잰 방법 틀림'으로 찍습니다. |
| 숍스 상품 ID로 `Mercapi.item()` 부르기 | 숍스 응답에는 `"data"` 키가 없어 KeyError로 터집니다. 재출품 오판이 몰리는 판매자(119903670)의 매물이 전부 숍스라, 가르지 않으면 확인이 **한 건도** 답을 못 냅니다. 엔드포인트를 가르는 자리는 `listing_response()` 하나뿐입니다 — 거기를 쓰세요. |
| 매물이 없어진 것을 `item()`/`product()`가 **None**을 돌려주는 것으로 알아내기 | 그 둘은 **404일 때만** None입니다. 실측(2026-09-14): 없어진 일반 매물은 **403**, 숍스만 404입니다. 400은 'ID 형식이 틀림'이라 또 다른 답입니다. 그래서 봇은 상태 코드를 직접 읽습니다(`listing_presence`). 이걸 모르면 없어진 매물이 전부 KeyError로 와서 **'없음'과 '못 물어봄'이 한 덩어리**가 됩니다. |
| 403을 곧바로 '사라짐'으로 믿기 | 403은 메루카리가 **우리를 막을 때도** 나옵니다. 그러면 모든 판정이 '사라졌다'로 떨어지면서 별개 매물의 신규 알림이 조용히 삼켜집니다. `lookup_survival()`이 매번 '지금 검색에 걸린 매물'을 눈금으로 먼저 물어보는 이유입니다 — 눈금이 '있음'으로 안 나오면 그 엔드포인트의 답은 전부 버립니다. |
| 지문에 저장된 가격을 **원본처럼 쓰기** | 지문 값은 `seen[item_id]`에서 베낀 **사본**입니다. 판매자가 값을 내리면서 제목을 고치면 지문 키가 새로 생기고 **옛 키가 옛 가격을 들고 남습니다**(실측 178건, 전부 제목 변경 / 177건은 인하 알림이 나간 뒤). 그 옛 키에 새 매물이 걸리면 이미 알린 값보다 비싼 기준가를 물려받아 PR #23의 결함이 재출품 경로로 되살아납니다. 주인이 아직 `seen`에 있으면 **원본을 보세요**(`relist_baseline`). 저장된 값은 고치지 마세요 — 옛 키는 다시 관측되지 않아 곧 다시 어긋나고, 178건이 매 실행 들락거리면 용량만 먹습니다. |
| 방향이 하나뿐인 값을 **주인이 누구인지 보지 않고** 합치기 | 기준가는 **매물 하나에 붙은 값**입니다. `seen`은 키가 곧 매물 ID라 안전하지만, 재출품 지문은 주인(`item_id`)이 값 **안에** 있어 두 실행이 다른 매물을 가리킬 수 있습니다. PR #34가 거기까지 '둘 중 낮은 쪽'을 걸어서, 살아남은 매물이 **남의 기준가**를 물려받았습니다(머지 뒤 8.4시간에 7건). 값이 스스로 모순이 됩니다 — 한 번도 알린 적 없는 가격이 '역대 최저가'로 앉고 그게 마지막 관측가보다 쌉니다. `same_subject`로 가릅니다. |
| 병합에서 '다른 질문이니 합칠 수 없다'를 **`mine`으로 끝내기** | `mine`은 그 매물을 이번 실행에서 **보지도 않은 쪽**일 수 있습니다. 판정 보류의 시계가 두 옛 판 사이를 16분 동안 13번 오갔고, `checks`가 `MIN_RELIST_ABSENCE_CHECKS`(2)에 영영 닿지 못했습니다(96건 중 77건). 합칠 수 없어도 **누가 더 최근에 봤는지**는 고를 수 있습니다(`checked_at`). 그리고 `fresh`처럼 질문이 아니라 **매물 자신의 성질**인 값은 어느 쪽을 고르든 살려야 합니다 — 꺼지면 신규 알림이 조용히 삼켜집니다(`m51322745277` 실제 사례). |
| 상태 값이 한 방향으로만 움직인다고 믿을 때, **판정 코드만 보기** | 상태 파일은 두 실행이 겹치면 `merge_seen.py`가 **밖에서** 합칩니다. `merge_ordered()`는 양쪽에 다 있는 키를 `mine`으로 덮는데, 내 실행이 먼저 시작했으면 그 값은 옛날 것입니다. PR #23이 판정 안에서 기준가 상승을 막은 뒤에도 병합 경로로 **245건**이 더 올라갔습니다(전부 `queue Mercari alerts` 커밋). 방향이 하나뿐인 값은 병합에서도 그 방향을 지켜야 합니다(`merge_price_record`). |
| 확인 방법을 '실행 로그에서 한 줄 세기'로 잡기 | 봇이 1분마다 도니 하루면 실행이 1,400번입니다. 개발 환경에서 Actions 로그 묶음 내려받기가 프록시에 막혀 있어 로그는 **한 건씩 API로만** 꺼낼 수 있습니다. 하루치를 세는 것은 사실상 불가능합니다. 세고 싶은 숫자는 **상태 커밋 메시지에 실으세요** — 자리는 이미 있습니다(`check_mercari.py`의 '실행 계수기' → `scripts/push_state.sh` → `scripts/commit_notes.py`). 상태 파일은 안 불고, 실을 것이 0이면 아무것도 안 붙습니다. |
| '상태 파일에 흔적이 없다'를 **판정 코드를 안 보고** 단정하기 | 이 README에도 요청문에도 "'못 물어봄'은 상태 파일에 흔적이 없다"고 적혀 있었는데 **틀렸습니다.** 답이 안 온 조회는 `checks`를 올리고 그 매물을 `pending_relists`에 **남깁니다**. 그 자국으로 PR #32 뒤 24시간을 세니 새 보류 0건 = 못 물어봄 0건이었습니다. 흔적이 없다고 적기 전에 그 값을 건드리는 함수를 먼저 읽으세요. |
| 자체 점검의 눈금 숫자를 **전부 한 자리로** 적기 | `commit_notes.py`의 `--calibrate`가 `못물어봄(?P<unknown>\d+)`을 `\d`로 줄여 놓아도 **그대로 통과했습니다.** 눈금 값이 전부 한 자리였기 때문입니다. 운영에서는 두 자리가 나오는 값이라 '12건'이 '1건'으로 세질 뻔했습니다. 눈금에는 **자릿수·경계·0을 섞으세요.** |
| 자릿수를 **섞어 두는 것으로** 끝내기 | 2026-09-17에 새로 짠 눈금이 값을 0부터 일곱 자리까지 섞어 뒀는데도 **자릿수 고장 둘을 통과**시켰습니다(천 단위 반올림 · 여섯 자리 자르기). 크기만 섞는 것으로는 안 됩니다 — **그 고장 아래에서 두 값이 같은 값으로 뭉개지는 판**을 놓아야 잡힙니다(999/1,001, 1,250,000/250,000). 네 라운드째입니다. |
| 자체 점검을 **한 칸만** 고치고 끝내기 | 2026-09-15에 `\d+`->`\d`를 겪고 두 자리를 섞었는데 **재출품 칸에만** 섞었습니다. `조회실패 (?P<failures>\d)`·`다음페이지 (?P<fired>\d)회`는 2026-09-17까지 그대로 통과했습니다. 고장은 **칸마다** 넣어 보세요. 그리고 **눈금이 부르지 않는 코드는 눈금이 못 지킵니다** — `walk()`(`--first-parent`)와 `report()`(비율의 분모)가 그랬습니다. `tests/test_commit_notes.py`의 `CalibrationCatchesFaultsTests`가 이제 고장 6개를 CI에서 계속 넣어 봅니다. |
| 짧은 창에서 나온 비율을 **'실측'이라는 이유로** 결론에 쓰기 | 꼬리표가 붙는 실행을 추정 12.6%에서 "실측 38%"로 덮었는데 그 38%가 **1.8시간**짜리였습니다. 43.3시간으로 재니 **16.0%** — 덮어쓴 값이 원래 추정보다 **더 틀렸습니다.** 하필 그 1.8시간이 하루 중 가장 바쁜 대(UTC 16~18시)였습니다. **추정을 실측으로 덮을 때, 그 실측의 창이 결론을 감당하는지 같이 보세요.** |
| `MAX_RELIST_LOOKUPS_PER_RUN`(12)이 '평소에 안 닿는다'고 믿기 | 근거가 '관측 최대 4건의 3배'였는데 2026-09-16 18:15에 **한 판 30건**이 왔습니다(정지 없음 — 간격 53~92초). 43.3시간에 5판이 상한에 걸려 **30건을 아예 못 물어봤습니다.** 더 나쁜 것은 주석의 "넘친 것은 다음 실행에서 다시 봅니다"도 틀렸다는 것입니다 — 넘친 매물이 검색 창에서 밀려나면 다시 물을 일이 없어 보류가 굳었다가 24시간 TTL로 **결론 없이** 사라집니다(실측: 18건이 17.3시간 동안 `checked_at` 정지, 앞선 3건은 TTL로 소멸). 상한을 올리려면 **조회 한 건의 왕복 시간**부터 재세요 — 20초 상한이 대신 걸리면 올려도 안 듣습니다. **2026-09-17에 그 시간을 꼬리표에 싣기 시작했습니다**(README "조회 한 건의 왕복 시간을 재기 시작했습니다"). 며칠 모인 뒤 `commit_notes.py`로 읽으세요. |
| 꼬리표 한 조각의 비용을 '꼬리표 하나 붙이는 값'으로 셈하기 | 61.6바이트/꼬리표는 제목이 전부 똑같던 커밋에 꼬리표를 **새로 만들어 붙일 때**의 값입니다. 이미 꼬리표가 붙은 커밋을 **늘리는 것**은 다릅니다 — 끊을 델타가 이미 끊겨 있고 늘어나는 글자의 대부분이 고정 문구라, 2026-09-17 실측에서 35바이트를 더했더니 팩이 **조각당 38바이트 줄었습니다**(같은 길이의 무작위 문자열은 +37바이트). **비싼 것은 길이가 아니라 '매번 다름'입니다.** |
| 실행당 상한(12건)을 '왕복 시간만 재면 올릴 수 있다'고 보기 | 걸린 시간의 대부분은 네트워크가 아니라 **조회 사이 간격**입니다. 왕복이 0초라도 잠든 시간만 `1.0초 x (눈금 조회 + N - 2)`라 20초 안에 드는 N은 **아무리 좋아야 19건**인데, 실측 최대는 **한 판 30건**입니다. 건수 상한으로는 그 봉우리를 못 덮습니다 — 간격을 보든지, 넘친 자리를 다음 실행이 확실히 다시 보게 만들어야 합니다. |
| 구간 epoch를 손으로 계산하기 | 눈금 구간을 하루 어긋나게 잡아 176건이 29건으로 나왔습니다. 그대로 갔으면 "머지 전에도 원래 적었다"는 엉뚱한 결론이 나올 뻔했습니다. `datetime.fromisoformat`으로 계산하고, **아는 값(176건)에 먼저 대 보세요.** |
| 정지 뒤 유실을 '평소 시간당 비율 × 정지 시간'으로 재기 | 유입이 시각대마다 크게 다릅니다(UTC 19시 9건/시 ~ 11시 194건/시). 정지가 한가한 시각대에 걸리면 기대치가 통째로 부풀어 없는 유실이 잡힙니다 — 실제로 "436건 중 263건 유실"이라는 답이 나올 뻔했습니다. **다른 날 같은 시계 구간**과 대세요. 다만 그 방법도 날짜 간 변동이 커서(140~310건) 큰 효과만 잡힙니다. |
| 봇이 잰 값을 **봇이 무엇과 비교했는지** 보지 않고 믿기 | `actions/checkout`의 기본값은 실행이 **만들어질 때**의 sha입니다. 한 실행이 50~90초 걸리고 트리거는 1분마다 오니, 실측 **85.5%**의 실행이 직전 실행이 끝나기 전에 만들어졌고 그 결과 **72.35%**가 직전 실행의 커밋을 못 읽은 채 판정했습니다. 잰 간격이 **실제 + 직전 실행의 길이**가 되어 180초 초과가 참 34건인데 봇은 291건으로 봤습니다(8.6배). PR #45에서 `ref: ${{ github.ref_name }}` 로 고쳤습니다 — `concurrency`가 실행을 직렬화하므로 체크아웃 **단계**가 도는 시점의 브랜치 끝이 곧 직전 실행의 최종 상태입니다. 고침이 들었는지는 `live_paths_audit.py`의 **부풀림 배수**가 1.0인지로만 확인됩니다. |
| 낡은 체크아웃의 피해를 **cadence 만**으로 보기 | 같은 낡은 읽기가 **인하 알림을 새게 하고 있었습니다(17건, 09-17에만 4건).** 그중 4건은 값이 **올랐는데** '인하'였습니다. `new:` 는 id가 매물 ID 하나뿐이라 `sent_alerts` 합집합이 막는데, `drop:` 는 id에 **기준가가 박혀 있어** 기준가가 낡으면 id가 달라지고 방어선이 통과시킵니다. **0건을 읽기 전에 그 방어선이 '무엇으로' 막는지 보세요** — 같은 자리에서 한쪽은 0건, 다른 쪽은 17건이었습니다. |
| 문턱을 옮길 수 있는지를 **정상 쪽 분포만 보고** 정하기 | `RUN_INTERVAL_SLACK`을 넓혀 부풀림을 덮으려 했는데, 고장 쪽을 재니 **겹쳐서 불가능**했습니다. 정상 실행이 봇에게 보이는 간격이 최대 **443초**인데, 외부 cron이 죽어 있던 13.9시간의 실제 간격은 **최소 398초**입니다. 자를 고치면 180초 vs 398초로 갈립니다. **감지하려는 고장의 실제 모양을 먼저 재세요** — README에 '최대 5분'이라 적혀 있었지만 실측 중앙값은 **632초**였습니다(`*/5` 스케줄이 제때 오지 않습니다). |
| 감지 장치의 '0건'을 그 장치가 **볼 자리**를 세지 않고 읽기 | 평소엔 안 도는 경로라 "잘 돈다"와 "한 번도 불린 적 없다"가 **같은 0**입니다. 2026-09-17 실측: `empty_feed_alerts`·`created_coverage_alerts`·`state_capacity_alerts`·`keyword_health_alerts` 넷 다 **자리 0회**라 제대로 도는지 아직 모릅니다. `scripts/live_paths_audit.py`가 분모와 분자를 같이 찍습니다. |
| 워치독이 `pending`을 안 건드린다고 **상태 필터만 보고** 믿기 | `?status=queued`가 잡 레코드 없는 실행(#7646)을 4일 넘게 돌려줬습니다. 갈라 주는 것은 **잡 레코드의 유무**입니다 — `has_runner()`는 빈 목록을 `any([]) is False`로 통과시킵니다. 멀쩡한 대기 실행을 안 죽인 것은 우리 판정이 아니라 **GitHub의 HTTP 409**였습니다. |
| 시험의 **정리(teardown) 실패**를 사소하게 보기 | 봇 워크플로가 매 실행 전에 테스트를 돌리므로 정리 실패가 곧 **그 1분의 결행**입니다. `test_push_state`의 임시 디렉터리가 git과 경쟁해 `OSError: [Errno 39] Directory not empty`로 터졌고, 사흘 반 동안 **15회** 봇이 조회·전송을 건너뛰고 텔레그램 '실행 실패' 알림을 15건 보냈습니다. 시험 본문은 전부 통과한 뒤였습니다. |
| `merge_audit.py`의 0건을 **'판정도 깨끗하다'**로 읽기 | 그 도구는 **병합이 값을 뒤집었는가**만 셉니다. 판정이 **낡은 값을 읽은 것**은 병합이 뒤에서 기워 주므로 상태 파일에 뒤집힘으로 남지 않습니다. 2026-09-18 실측: PR #35 이후 구간이 `기준가 상승 0 / 보류 시계 후퇴 0 / fresh 소실 0`으로 깨끗한데, 같은 구간에서 **인하 알림은 17건이 낡은 기준가로 나갔습니다.** 알림 흐름(`sent_alerts`의 `drop:` id를 매물별 시간순으로)을 따로 붙여야 갈립니다. |
| 병합이 **어떻게** 합치는지 안 보고 '뒤집힘 0건'을 읽기 | `merge_seen.py`는 `sent_alerts`를 **합집합**으로, `keyword_checked_at`을 **더 늦은 쪽**으로 합칩니다. 그래서 이 둘이 뒤로 간 판이 0건인 것은 '막았다'가 아니라 **'그 자리로는 일어날 수 없다'**입니다. 0을 읽기 전에 합치는 함수를 보세요. |
| 창 크기를 '순위 N번째 매물의 나이'로 재기 | 등록순 결과는 등록 시각 순이 아닙니다(실측: Margiela 597건 중 역전 254건). 그 자리에 예전 매물이 앉으면 값이 통째로 널뜁니다. **'창 밖으로 밀려난 것 중 가장 최근 매물의 나이'**로 재세요 — 최솟값이라 섞여 들어온 예전 매물에 흔들리지 않습니다. |

## 운영 이력을 훑을 때

상태 파일 이력이 이 저장소의 주된 증거입니다. 훑는 방법 자체에 함정이 있습니다.

- **재출품 판정을 다시 셀 일이면 `scripts/relist_audit.py`가 이미 있습니다.** PR #25의
  계산을 그대로 담았고, `--calibrate`가 그때의 눈금(판정 68건 / 첫 관측 인하 19건)과
  맞는지 확인하고 어긋나면 1로 끝납니다. 얕은 클론이면 `git fetch --unshallow origin main`
  을 먼저 하세요.
- **재출품이 물려받은 기준가가 `seen` 원본과 맞는지 셀 일이면
  `scripts/relist_inherit_audit.py`가 이미 있습니다.** PR #37의 실전 확인용이고,
  `--calibrate`가 아는 답 **넷**(판 수 7,338 / 자리 257 / 비싼 쪽 1 / 그 한 건의
  215,000·200,000)을 함께 확인합니다. 네 값이 **같이** 맞아야 합니다 — 창을 3분만 넓히면
  한 판에 몰린 32건이 들어와 257이 289가 됩니다. 도구는 자리를 **세 층**으로 갈라 찍고,
  3층(= 사본이 쓰였는지 가려진 자리)이 0이면 표를 안 찍고 '아직 못 잼'으로 끝냅니다.
  `merge_audit.py`의 `지문>매물`과 **섞지 마세요** — 그쪽은 머지 뒤에도 계속 나오는 것이
  정상이고(저장된 값을 안 고치는 것이 PR #37의 선택), 이제 '판매자가 제목을 고친 횟수'로
  읽습니다.
- **cadence의 자가 부풀어 있는지 볼 일이면 `live_paths_audit.py`의 `[cadence 의 자(尺)]`
  절입니다.** 봇이 커밋한 `__last_cadence_ok__`가 그 실행의 `__last_search_ok__`와 같은가가
  곧 '봇이 본 간격 <= 180인가'이고, 그것을 커밋 이력의 **참 간격**과 맞대 **부풀림 배수**를
  찍습니다(1.0이면 자가 맞습니다). `__last_cadence_ok__`가 없던 2026-09-12 18:49:36 이전
  구간은 **'측정 없음'으로 갈라 찍습니다** — 안 가르면 999판이 통째로 '정지'로 세지고,
  실제로 그 고장을 냈습니다(18.4% vs 4.62%).
- **평소엔 안 도는 경로가 실제로 돌았는지 볼 일이면 `scripts/live_paths_audit.py`가
  이미 있습니다.** 경로마다 **자리(분모)와 울림(분자)을 같이** 찍습니다 — 자리가 0이면
  울림 0은 '이상 없음'이 아니라 '아직 못 잼'이라고 대놓고 씁니다. `--calibrate`가 창
  **둘**에서 아는 답 39개를 함께 확인하고 어긋나면 1로 끝납니다(창을 둘 두는 이유는
  한 창 안에서 15분 문턱과 30분 문턱이 둘 다 1건으로 뭉개지기 때문입니다).
  `keyword_checked_at`이 없던 2026-09-12 02:31 이전 구간은 **'측정 없음'으로 갈라
  찍습니다** — 만들면서 그 경계에서 12,947판을 조용히 잃는 고장을 실제로 냈고,
  눈금의 '시계를 읽은 판 수'가 그것을 잡았습니다.
- **실행 로그에만 있던 숫자를 셀 일이면 `scripts/commit_notes.py`가 이미 있습니다.**
  봇이 상태 커밋 제목 끝에 실어 둔 꼬리표(`(재출품 5: 있음2/없음3/못물어봄0 …)`)를 `git log`로
  셉니다. `--calibrate`가 봇이 쓰는 쪽과의 왕복까지 확인하고 어긋나면 1로 끝납니다.
  **꼬리표가 없는 구간의 0을 읽지 마세요** — 계수기가 배포된 2026-09-15 이전 구간은
  '0건'이 아니라 '측정이 없음'이고, 도구가 그 둘을 갈라 찍습니다.
  `직접조회 N초/왕복M초` 칸(2026-09-17 추가)은 **판마다** 들고 중앙값·p90·최대를 찍고,
  외삽하기 전에 **식을 실측에 되짚어** 봅니다(그 판의 눈금 조회 수가 1~2건으로 나오는지).
  안 맞으면 표를 안 찍고 ⛔로 끝냅니다.
  `--by-hour`로 시각대를 가르고, `못 물어봄`(물어봤는데 답이 안 옴)과 `아예 못 물어봄`
  (상한에 걸려 묻지도 못함)을 **섞지 마세요** — 앞쪽만 차단의 신호입니다. 뒤쪽은
  도구가 다시 '건수 상한'과 '시간 상한'으로 가릅니다(한 판의 '물어본 것'이 상한과
  같으면 건수, 작으면 시간).
- **병합이 값을 뒤집었는지 셀 일이면 `scripts/merge_audit.py`가 이미 있습니다.**
  `--calibrate`가 아는 답(176건 / 245건)과 **자체 점검**(일부러 어긋낸 판을 넣어 세는 자가
  잡는지)을 함께 확인하고 어긋나면 1로 끝납니다. 구간을 자를 땐 **앞 한 판을 씨앗으로**
  가져와야 합니다 — 구간 첫 판은 비교 상대가 없어 경계에서 한 건이 조용히 빠집니다(245가
  244로 나왔습니다). 그리고 `collections.Counter`는 **0을 더해도 키가 남습니다**. 그대로
  두면 '아무 일도 없었다'가 빈 Counter 가 아니라서 거짓 양성 점검이 언제나 통과합니다.
- **`git log --first-parent`를 쓰세요.** 그냥 `git log`를 쓰면 PR 브랜치의 커밋이
  섞여 들어오는데, 그 커밋들의 상태 파일은 브랜치를 딴 시점의 **옛날 것**이라 상태가
  뒤로 갔다 오는 것처럼 보입니다. 실제로 이것 때문에 "전송 기록 유실 930건"이 잡혔고
  전부 허깨비였습니다(해당 커밋들은 상태 파일을 건드리지도 않았습니다).
- **`seen_items.json`을 파일 읽기 도구로 열지 마세요.** 2.3MB짜리 한 줄 JSON입니다.
  `python3`으로 파싱해서 필요한 부분만 보세요.
- **전량을 훑을 땐 `git cat-file --batch`로 스트리밍**하세요. 커밋마다 체크아웃하면
  몇 시간이 걸립니다. 1만 판을 훑는 데 4분이면 됩니다.
- **"그 매물이 그때 관측됐는가"를 dict 순서로 복원하지 마세요.** `remember()`가 다시 본
  항목을 뒤로 보내므로 순서가 곧 관측 기록처럼 보이지만, 병합과 실행 겹침이 순서를
  되돌려 놓아 **거짓 양성**이 납니다. 2026-09-13에 그 방식 두 가지(갱신 블록 복원,
  위치 감소)가 둘 다 "재출품 판정 68건 전부 오판"이라는 틀린 답을 냈습니다. 그나마
  나은 신호가 **지문의 소유자가 그 ID로 바뀌는 순간**입니다 — 두 매물이 같은 실행에서
  함께 관측되면 나중 것이 덮어써서 가려지므로 그 신호로 센 값은 **하한**입니다.
- **그 신호에 "거짓 양성이 없다"고 적어 뒀던 것은 틀렸습니다(2026-09-15 반증).**
  병합이 지문 주인을 **예전 ID로 되돌려 놓습니다.** `merge_ordered()`가 양쪽에 다 있는
  키를 `mine`으로 덮는데 내 실행이 먼저 시작했으면 그 값은 옛날 주인이라, 예전 매물을
  다시 본 적이 없어도 '다시 관측됨'으로 읽힙니다. `relist_audit.py`가 이것 하나로
  "오판 1건"을 보고했는데, 그 매물을 메루카리에 직접 물어보니 **없었습니다**(HTTP 403,
  같은 실행에서 살아 있는 매물 3건은 '있음'으로 답해 차단이 아님을 확인). 오판은
  0건이었습니다. 되돌림을 가려내려면 **예전 매물이 `seen`의 끝으로 갔는지**를 같이
  보세요(관측되면 `remember()`가 끝으로 보냅니다). 그 건은 480판 내내 끝에서 3,118번째에
  붙박여 있었습니다.

## 오래 걸리는 작업을 기다릴 때

```bash
# 이렇게 하면 안 됩니다 — 이 셸 자신의 명령줄에 "walk.py"가 들어 있어서
# pgrep이 자기 자신을 찾아냅니다. 작업이 끝나도 대기가 영영 안 끝납니다.
until ! pgrep -f "walk.py" >/dev/null; do sleep 5; done

# PID로 기다리거나
while kill -0 "$PID" 2>/dev/null; do sleep 5; done
# 작업이 마지막에 남기는 완료 표시를 보세요
until grep -q "완료" run.log; do sleep 5; done
```

## 용량을 잴 때

README "저장소 용량 관리"에 방법과 함정이 정리돼 있습니다. 요점만:

- **배포 전후 비교로 재지 마세요.** 창 크기만 바꿔도 값이 널뜁니다. 운영 이력의 상태
  파일을 가져다 **내용은 그대로 두고 배치만 바꿔** 같은 방식으로 팩하세요.
- **`git repack`에 `-f`를 주세요.** 빼면 fast-import가 만든 델타를 재사용해 팩이 2배
  가까이 부풉니다.
- **상한을 낮추는 게 아끼는 길이라고 생각하지 마세요.** 2026-09-14 실측: 상한을 13,000으로
  자르면 +0.04%인데 8,000이면 **+29.7%**, 6,000이면 **+148.9%**입니다. 다시 볼 일 없는 꼬리를
  들고 있는 비용은 일회성이고 작지만(델타가 건너뜁니다), 상한이 아직 다시 관측되는 구간까지
  파고들면 경계 항목이 매 실행 들락날락하며 파일 앞쪽이 통째로 다시 쓰입니다.
- **대조군(내용이 같은 변형본)을 두 개 재세요.** 같은 입력인데도 팩 크기가 달라집니다.
  2026-09-13 실측에서는 바이트까지 같은 대조군 두 개가 **6.5%** 벌어졌습니다(값이 두
  군데로 뭉칩니다). 하나만 두면 그 한 번이 어느 쪽에 떨어지느냐에 따라 결론이 뒤집힙니다.
  **잡음 바닥 안쪽 차이는 의미가 없습니다.**

## 테스트

```bash
python -m unittest discover -s tests     # 봇이 매 실행 전에 돌리는 것과 같은 명령
python scripts/clock_scan.py --day       # 시각을 바꿔 가며 (PR CI에서 돌아감)
python scripts/clock_scan.py --repeat 100
```

`tests/test_mercapi_surface.py`만 **깨끗한 인터프리터를 따로 띄웁니다** —
`test_check_mercari.py`가 mercapi를 스텁으로 바꿔치기하기 때문입니다.

## 다음에 무엇을 할지

상황별 요청문과 지난 라운드 경위는 소유자의 아티팩트에 정리돼 있습니다.
소유자가 어느 작업을 할지 지정해 줄 것입니다.

<https://claude.ai/code/artifact/8328e202-65cc-440b-afef-d3a9a20aafc5>

재출품 오판은 고쳤습니다(README "재출품 감지"). 계획돼 있던 '매물별 마지막으로 본 시각'은
**두 가지 이유로 쓰지 않았습니다** — 용량이 반올림을 아무리 거칠게 해도 +20% 아래로
안 내려가고, 더 중요하게는 그 값으로 진짜 재출품과 오판이 **가려지지 않습니다**(둘 다
예전 매물이 직전까지 관측됩니다).

그다음 라운드에서 **판정을 대리 지표에서 뗐습니다**(README "재출품 판정을 대리 지표에서
뗐습니다"). 사라짐을 시간으로 확인하던 방식은 걷어냈습니다 — 그 방식이 기대던 '이번
결과에 없음'이 애초에 증거가 아니었기 때문입니다. 지금은 판정 직전에 예전 매물에게
직접 묻고, **못 물어봤으면 '사라짐'이 아니라 '별개의 매물'**로 끝냅니다.

시야 범위도 쟀습니다(README "봇이 '볼 수 있는 것'을 보는가"). **등록순 창(상위 120건)은
문제가 아니었고**(못 본 것 0건), 카테고리 필터도 소유자가 고른 카테고리 안에서는 빠뜨리는
것이 없습니다. 창을 줄이는 것은 아끼는 게 없고 손해만 납니다.

추천순(score) 창도 쟀습니다(README "추천순(score) 창은 등록순과 …"). **용의자가 맞긴
한데 물음이 좁았습니다.** 추적 중인 매물 가운데 봇의 창 안에 들어와 있는 것은 (이미 팔린
것을 걸러내고도) **34~36%뿐**입니다. 즉 재출품 판정이 사라짐의 증거로 삼는
`matched_id not in listed_ids`는 거의 아무 정보도 담고 있지 않습니다. 추천순 창은 그중
한 겹일 뿐이고, 넓혀도 못 고칩니다.

**그렇다고 빼도 되는 것은 아닙니다.** 두 패스의 창은 **30%만 겹칩니다**(1,840건 중 552건).
추천순을 빼면 창 안 추적 매물의 **44%(690건)**가 눈에서 사라지고, 그 매물들의 가격 인하는
아무도 안 보게 됩니다(새 매물 탐지는 등록순이 책임지므로 그쪽은 영향 없음). 넓혀도 못
고치고 빼면 손해입니다 — **고칠 곳은 창이 아니라 판정입니다.**

1번은 했습니다(위 문단).

**병합에서 뒤집히는 값도 전수로 셌습니다**(README "병합에서 뒤집히는 값을 전수로 셌습니다").
둘이 더 나왔고 둘 다 알림이 **조용히 삼켜지는** 쪽입니다 — 지문이 다른 매물의 기준가를
물려받는 자리(PR #34가 만든 자리)와, 판정 보류의 시계가 두 옛 판 사이를 오가는 자리입니다.
후자는 PR #34의 설명을 한 겹 고칩니다: 보류가 안 끝난 것이 '다시 안 나타나서'만은
아니었습니다. 1분마다 꼬박꼬박 다시 관측되는데도 병합이 시계를 되돌리고 있었습니다.
`seen`의 **순서**와 지문의 **주인**은 뒤집힐 수 있지만 고치지 않았습니다 — 각각 '상한에
아직 안 닿아서 잴 수 없음'과 '고를 근거(시각)가 없음'입니다.

`wants_another_page()`도 고쳤습니다(README "다음 페이지 조건 …"). 발동 조건을 '가장
오래된 매물'에서 **'이 페이지의 신규 매물 개수'**로 바꿨습니다. 재 보니 예상이 하나
틀렸습니다 — 실제로 있었던 7.34시간 정지에서는 **아무것도 새지 않았습니다.** 위험은
'몇 시간 멈췄는가'가 아니라 **'멈춘 동안이 바쁜 시각대와 겹쳤는가'**입니다(Hermes는
시간당 최대 122건이라 1시간이면 창 120칸이 찹니다).

**셋 다 바쁜 시각대를 지나고 나서 실측했습니다**(README "PR #34·#35 뒤 다시 쟀습니다").

- **기준가 상승 0건.** 같은 시계 구간(UTC 05:00~13:05)이 09-13에 133건, 09-14에 171건인
  자리입니다. PR #34가 만들었던 '지문<매물'도 18건 -> 0건입니다.
- **'지문>매물' 어긋남만 안 변했습니다**(36 / 22 / 21건). PR #35가 만든 것도 고친 것도
  아니고, 정체를 아직 모릅니다. 알림이 한 건 더 가는 쪽이라 급하진 않습니다.
- **재출품 판정 7건 전부 옳았습니다.** 예전 매물 7건을 이름 대고 물으니 전부 없음(일반
  5건 403 / 숍스 2건 404). 같은 실행에 **살아 있는 매물 3건**을 섞어 물어 차단이 아님을
  먼저 확인했습니다 — 없는 ID만으로 눈금을 삼으면 차단당했을 때도 통과합니다.
  `pending_relists`는 0건입니다.
- **다음 페이지 조건은 아직 발동 0회.** 상태 커밋 간격 최대가 175초라 정지가 없었고,
  그러면 발동할 일도 없습니다. 판단은 긴 정지가 바쁜 시각대에 걸릴 때까지 미룹니다.

**못 세던 숫자를 세게 만들었습니다**(README "못 세는 숫자를 세게 만들었습니다"). `[재출품]
… 못 물어봄`을 비롯해 상태 파일에 흔적이 없는 값들을 상태 커밋 제목 끝에 싣고,
`scripts/commit_notes.py`로 셉니다. 비용은 실측했습니다 — 값이 매번 다른 꼬리표는 팩에서
**원문보다 더 듭니다**(원문 46.8바이트에 팩 61.6바이트). 붙는 실행은 **43.3시간 실측으로
16.0%**(360/2,247)라 하루 약 12KB, 저장소 하루 증가분의 **0.12%**입니다. 같은 측정에서
**고정 문구는 팩을 오히려 10% 줄였습니다**(같은 문자열이 길수록 델타가 먹을 것이 많습니다).
이 자리에 한때 '38%'가 적혀 있었는데 **1.8시간짜리 창**이었습니다.

그 과정에서 '흔적이 없다'가 절반은 틀렸다는 것이 나왔습니다 — 답이 안 온 조회는
`pending_relists`에 남습니다. 그것으로 세니 PR #32 뒤 24시간 동안 **못 물어봄 0건 / 눈금
불신 0회 / 상한 초과 0회**입니다.

**조회 한 건의 왕복 시간을 재기 시작했습니다**(README "조회 한 건의 왕복 시간을 재기
시작했습니다"). 실행당 상한(12건)을 올릴지가 이 숫자 하나에 막혀 있었습니다. 꼬리표에
`직접조회 13.6초/왕복0.10초`를 싣고, `commit_notes.py`가 중앙값·p90·최대와 '상한을 N으로
올리면 몇 초'를 찍습니다. **'상한에 걸린 판에만 싣자'는 아낌수는 재 보고 버렸습니다** —
이미 꼬리표가 붙은 커밋을 늘리는 것은 팩을 오히려 **조각당 38바이트 줄입니다**.
그리고 **재기 전에 하나가 이미 갈렸습니다**: 왕복이 0초라도 조회 사이 간격만으로
건수는 **19가 천장**이라, 실측 최대인 한 판 30건은 건수 상한으로 못 덮습니다.

**배포 뒤 1.8시간 실측으로 분모까지 나왔습니다** — 물어본 74건 중 있음 70 / 없어짐 4 /
**못 물어봄 0건(0.00%)**, 눈금⛔ 0회, 조회 실패 0건. 그 0이 침묵이 아닌 근거는 같은 줄의
'있음 70 · 없어짐 4'입니다. 비율이 안정되려면 며칠치가 더 필요합니다.
