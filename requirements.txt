# 상한을 둡니다. 이 봇은 1분마다 스스로 배포되는 구조라, 범위를 열어 두면 새 릴리스가
# 나온 그 실행부터 검증 없이 운영에 들어갑니다. 올릴 때는 PR로 올려서 CI를 거칩니다.
#
# httpx는 0.x라 마이너 올림도 호환이 깨질 수 있어 패치 자리까지 고정합니다(>=0.28.1,<0.29).
# ~=0.28로 두면 0.29 이상도 허용되는데, 지금 0.28.1이 설치되는 건 mercapi가 스스로
# httpx<0.29를 요구하기 때문입니다. 즉 상한이 우리 것이 아니라 남의 것에 얹혀 있었고,
# mercapi가 범위를 넓히는 순간 검증 안 된 httpx가 들어옵니다.
#
# 두 줄은 함께 움직입니다. mercapi 0.5.0이 httpx>=0.28.1을 요구하므로, mercapi만 올리거나
# httpx만 올릴 수 없습니다(2026-09-12 0.4.2 -> 0.5.0 업그레이드가 그랬습니다).
mercapi~=0.5.0
httpx~=0.28.1
